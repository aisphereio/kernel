syntax = "proto3";

package aisphere.access.compat.v1;

option go_package = "github.com/aisphereio/kernel/aisphere/access/v1;accesscompatv1";

// Compatibility placeholder kept only to avoid a stale root import path.
// Use api/aisphere/access/v1/access.proto for the real aisphere.access.v1 policy option.
message DeprecatedAccessCompat {}
